# Copy to .worker-secrets and fill in, or let the app generate one on first run.
# File mode should be 0600. Never commit the real file.
#
# Use API_KEY as the X-API-Key header for HTTP and as gRPC metadata x-api-key.

API_KEY=your_api_key_here
JWT_SECRET=your_jwt_secret_here
